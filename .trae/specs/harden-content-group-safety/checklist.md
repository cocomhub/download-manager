- [x] 未解析合法组名的对象会生成非空且独立的 `unknown+title` / `unknown+url` 分组键
- [x] 每次启动都会全量重算 content_group；与存量不一致时会持久化纠偏
- [x] 组策略仅在相同 `task_id + task_type + content_group` 范围内生效
- [x] 同组同优先级存在多个对象时，该组自动取消策略被整体跳过
- [x] `downloading` 对象不会被仅通过状态覆盖方式自动取消
- [x] `group_by=content` 聚合不会把未知对象折叠为同一组，也不会跨任务串组
- [x] 组展开与前端批量取消操作附带任务维度，避免误操作其他任务对象
