- [x] 服务启动与停止流程不再读取、写入或创建任务级 `cache` 文件
- [x] 冷启动过程中不再对任务存储执行全量 `Search(nil)` 回灌对象到内存
- [x] 任务详情、全局聚合、内容分组与组展开改为通过 `Storage` 分页/过滤查询
- [x] 重试、取消、撤销取消等单对象操作可通过 `Storage` 精确定位目标对象
- [x] 任务增量刷新不再依赖全量 `knownURLs` / `objects` 常驻集合完成历史去重
- [x] `mongo` 列表与搜索请求在数据库侧完成过滤、排序、分页与计数
- [x] `mongo` 常规请求路径不再使用 `cursor.All` 拉取整个集合
- [x] 常用 `mongo` 查询具备索引或显式边界，避免请求主路径全表扫描
- [x] 大数据量场景下启动内存峰值明显下降，且接口行为与现有功能保持兼容
